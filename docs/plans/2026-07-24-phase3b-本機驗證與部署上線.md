# Phase 3b — 本機驗證與部署上線（進行中 ⬜）

> 承接 Phase 3a（憑證串接與 bot 品質修正，已完成，見 `archive/2026-07-24-phase3a-憑證串接與bot品質修正.md`）。8 個環境變數已全數就緒，bot 已能在本機正常運作。本計畫要完成剩下的驗證與正式上線。

## 目標

TESTING.md 本機清單全過（含第一次真正跑過 `/generate`）→ 部署 Vercel + 綁 webhook → 線上完成 TESTING.md 全清單 → Dashboard 上線可用。

## 現況（2026-07-24）

目前只用 Redis 直接檢查過對話內容驗證語氣/長度，`/generate`、`/start`、`/cancel`、`/list` 都**還沒有實際跑過一次**——這是接下來最優先的缺口，因為 `/generate` 三段鏈是整個專案的核心產出，至今尚未端對端驗證。（語音輸入功能已於 2026-08-07 移除，不再列入驗證項目。）

## 步驟

### 1. 本機閉環：跑完 TESTING.md 指令流程與辯論清單

- [ ] `/start`：確認歡迎訊息。
- [ ] `/movie <冷門片名>`：確認 OMDb miss 時 fallback 正常。
- [ ] `/cancel`：確認 session 清除。
- [ ] `/list`：確認空清單與有資料時的顯示都正常。
- [ ] 連續多輪辯論：確認 AI 記得前文脈絡。

### 2. 本機閉環：第一次真正跑 `/generate`

- [ ] 跟 AI 聊到至少 4 則往來 → `/generate` → 確認先收到 ack，背景處理完後收到 digest。
- [ ] digest 結構（盲點/對撞/重塑）與 Markdown 顯示正常。
- [ ] 完成後 session 已清除，`/list` 能看到剛歸檔的影評。
- [ ] 若 digest 超過 4096 字，確認自動切段送出。

### 3. Dashboard 本機驗證

- [ ] `npm run dev:full` 起前端 + api。
- [ ] 用 `DASHBOARD_TOKEN` 登入 TokenGate，確認能看到剛才 `/generate` 出來的真實資料（年份分組、genre 篩選、統計面板）。
- [ ] 375px 視窗版面確認。

### 4. 部署與 webhook 綁定

- [ ] 安裝 vercel CLI（本機目前沒有）或改用 git push 觸發自動部署。
- [ ] Vercel dashboard 設定全部 8 個環境變數（Upstash 兩項由 Marketplace 整合自動注入則免手動）。
- [ ] `vercel --prod` 部署。
- [ ] `setWebhook` 帶 `secret_token` + `drop_pending_updates=true` 綁定；`getWebhookInfo` 確認 `last_error_message` 為空。

### 5. 線上驗收

- [ ] 跑 TESTING.md「部署後（webhook 模式）」清單。
- [ ] 手機 Telegram 完整走一輪：`/movie` → 辯論 → `/generate` → `/list`。
- [ ] Dashboard 線上驗證：正確 token 進站、真實 reviews 渲染、375px 實機版面。

## 驗證清單（完成定義）

- [ ] TESTING.md 本機與線上清單全過。
- [ ] Dashboard 有公開網址可用，能看到真實歸檔資料。
- [ ] 完成後：FEATURES.md Phase 3 標 ✅、CHANGELOG 記錄上線、本檔移入 `archive/`。

## 備註

- README 的 Gemini 免費額度寫「每分鐘 15 次」是舊數字，現為 10 RPM / 約 250 RPD（一般 Flash 免費層數字；這個專案實測 `gemini-3.5-flash` 只有 20 次/天，見 Phase 3a 備註），可順手修正。
- Telegram 舊金鑰若有外洩疑慮，記得在 AI Studio 撤銷換掉的那把 Gemini 舊金鑰。
- 目前 20 次/天的額度是否為新專案的暫時性保守配額，還是長期如此，尚待時間驗證；已有 `gemini-3.1-flash-lite` 降級鏈頂著。
- `/movie` 目前沒有防呆機制，同時只能討論一部片，換片會直接覆蓋舊 session（見 Phase 3a 備註）。若之後想加確認提示，屬於新功能，另立計畫。
