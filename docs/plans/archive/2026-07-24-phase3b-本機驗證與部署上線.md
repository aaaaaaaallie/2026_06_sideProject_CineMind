# Phase 3b — 本機驗證與部署上線（已完成 ✅）

> 承接 Phase 3a（見 `archive/2026-07-24-phase3a-憑證串接與bot品質修正.md`）。8 個環境變數已就緒、bot 本機可正常運作，本計畫完成剩下的正式部署與線上驗證。完成日：2026-08-07。

## 目標

Vercel 部署 + webhook 綁定 → 正式環境跑通 `/movie` → 辯論 → `/generate` → `/list` 主線 → Dashboard 上線可用。

## 完成項目

### 1. 首次正式部署與 webhook 綁定

- 建立 Vercel 專案、設定 8 個環境變數、部署上線。
- 部署後 webhook 完全無回應（含完全不碰 Redis 的 `/start`），追查後定位到 Vercel 上的 `TELEGRAM_BOT_TOKEN` 環境變數**前面多了一個空白字元**，導致 Telegram `sendMessage` 回 404 Not Found；修正後 `setWebhook` + `getWebhookInfo` 確認 `last_error_message` 為空、回應時間從逾時 60 秒降到 1–2 秒。

### 2. 正式環境指令流程驗證

- `/start`、`/movie`（含 OMDb 查無此片與亂碼片名的防呆）、`/cancel`、`/list`：以模擬 webhook 呼叫與使用者實機操作交叉驗證，皆正常。
- 白名單無聲忽略：模擬非白名單 chat id，確認被靜默擋下、log 可見。
- `GET /api/reviews` 無 token／錯誤 token：皆正確回 401。

### 3. `/generate` 三段鏈完整主線驗證

- `/movie` → 多輪辯論 → `/generate` → `/list` 完整跑過一次：先收到 ack，背景處理完收到 digest，`/list` 能看到剛歸檔的影評。

### 4. Dashboard 線上驗證

- 正確 token 登入、真實歸檔資料渲染、刪除功能、編輯功能（片名／備註）、375px 行動版面皆已在正式環境驗證通過。

### 5. 過程中衍生的品質修正與功能（詳見 `docs/CHANGELOG.md` 2026-08-07 各條）

- `/movie` 對 OMDb 查無此片、以及 Gemini 對亂碼輸入硬猜片名的兩層防呆。
- 辯論人格與 `/generate` 文章 prompt 加入避免 AI 腔規則；文章新增劇情簡介段落；結尾強制要求「## 結論」與「一句話結語」。
- 語音輸入功能整個移除（不再使用）。
- Dashboard 新增刪除、編輯（片名／備註）功能。

## 未逐項驗證的邊角案例

以下風險較低，未在本次完整驗證，之後遇到問題再處理：

- 冪等鎖對「同一 update 重複投遞」的行為（邏輯已實作，未實際模擬重送）。
- Dashboard 的年份／genre 篩選、登出流程。
- 超長 digest（>4096 字）自動切段。

## 驗證

- 部署與 webhook：`getWebhookInfo` 確認 `url` 正確、無 `last_error_message`。
- 正式環境主線：`/movie` → 辯論 → `/generate` → `/list` 使用者實機操作全過。
- Dashboard：使用者實機確認登入、真實資料、刪除、編輯、375px 版面皆正常。
- 所有變更已同步進 `docs/CHANGELOG.md`、`docs/FEATURES.md`、`docs/ARCHITECTURE.md`、`docs/TESTING.md`。
